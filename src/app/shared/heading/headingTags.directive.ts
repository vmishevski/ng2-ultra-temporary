import { Directive, Component } from '@angular/core';

@Directive({selector: 'ult-heading-title'})
export class HeadingTitle {
}

@Directive({selector: 'ult-heading-actions'})
export class HeadingActions {
}
